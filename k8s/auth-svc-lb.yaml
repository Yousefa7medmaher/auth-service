apiVersion: v1
kind: Service
metadata:
  name: auth-svclb
  namespace: micro-app
spec:
  type: LoadBalancer
  selector:
    ecommerce-app: auth-service 
  ports:
    - protocol: TCP
      port: 5000        
      targetPort: 5000   
